pub mod cursor;
pub mod server;
pub mod client;
mod listener;
mod dispatcher;
mod object;
mod blob;
mod errors;


pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
